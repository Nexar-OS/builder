[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nexar-builder"
version = "0.1.0"
dependencies = [ "requests==2.34.2", "meson==1.12.0" ]
requires-python = ">=3.11"
authors = [ ]
description = ""
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]